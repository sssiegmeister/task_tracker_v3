<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>TaskTrackerV3 · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.scss") %>"/>
  </head>
  <body>
    <div class="container">
     <%= if get_flash(@conn, :info) || get_flash(@conn, :error) do %>
       <div class="row">
         <div class="col-12">
           <%= if get_flash(@conn, :info) do %>
             <p class="alert alert-info" role="alert">
               <%= get_flash(@conn, :info) %></p>
           <% end %>
           <%= if get_flash(@conn, :error) do %>
             <p class="alert alert-danger" role="alert">
               <%= get_flash(@conn, :error) %></p>
           <% end %>
         </div>
       </div>
     <% end %>
     <%= render @view_module, @view_template, assigns %>
   </div>
   <script type="text/javascript" src="<%= static_url(@conn, "/js/app.js") %>"></script>
  </body>
</html>
